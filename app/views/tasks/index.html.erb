<% @tasks.each do |task| %>
  <%= link_to task.name, task_path(task) %>
  <%= link_to "edit task", edit_task_path(task) %>
  <%= link_to "Delete", task_path(task), #autoextracts the id
      method: :delete,
      data: { confirm: "Are you sure?" } %>
      <%= link_to "create", new_task_path %>
<% end %>
